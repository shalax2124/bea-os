---
phase: 01-shell-and-auth
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified: []
autonomous: false
requirements: []

must_haves:
  truths:
    - "App is deployed to Vercel and accessible at bea-os.vercel.app"
    - "Unauthenticated users see Clerk sign-in page"
    - "Bea and Sharie can both log in and see the dashboard"
    - "User can sign out via UserButton"
  artifacts: []
  key_links: []
---

<objective>
Deploy the complete app to Vercel and verify end-to-end auth + dashboard shell.

Purpose: Prove the full stack works: Clerk auth + dashboard UI deployed and accessible to both users.
Output: Live app at bea-os.vercel.app.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-shell-and-auth/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy to Vercel with Clerk env vars</name>
  <files>None — deployment only</files>
  <read_first>.env.example (to know which env vars to set)</read_first>
  <action>
Deploy the complete app to Vercel:

1. Check if .env.local has real Clerk credentials (NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY should start with "pk_"). If placeholders, pause and ask Sharie to fill in real values from Clerk Dashboard.

2. Check if DATABASE_URL in .env.local has a real Neon connection string (should start with "postgresql://"). If placeholder, pause and ask Sharie.

3. Initialize git repo if not already: `git init && git add -A && git commit -m "feat: initial bea-os with Clerk auth + dashboard shell"`

4. Link to Vercel: `npx vercel link` (project name: bea-os)

5. Set env vars on Vercel (one by one):
   ```
   npx vercel env add NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY production
   npx vercel env add CLERK_SECRET_KEY production
   npx vercel env add NEXT_PUBLIC_CLERK_SIGN_IN_URL production  (value: /sign-in)
   npx vercel env add NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL production  (value: /dashboard)
   npx vercel env add DATABASE_URL production
   ```

6. Deploy to production: `npx vercel --prod`

7. Verify the deployment URL resolves: `curl -s -o /dev/null -w "%{http_code}" https://bea-os.vercel.app`

Important: After deployment, Sharie must also add the Vercel deployment URL to Clerk's "Allowed redirect URIs" in Clerk Dashboard -> Sessions -> Allowed redirect URLs. Add: https://bea-os.vercel.app

Also: Ensure two users exist in Clerk Dashboard -> Users:
- Bea (bea@ally.com or similar)
- Sharie
Both created with email + password.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" https://bea-os.vercel.app 2>/dev/null || echo "not-deployed-yet"</automated>
  </verify>
  <acceptance_criteria>
    - Vercel project linked as bea-os
    - All 5 env vars set on Vercel production
    - npx vercel --prod succeeds
    - bea-os.vercel.app responds (200 or 307 redirect to sign-in)
  </acceptance_criteria>
  <done>App deployed to bea-os.vercel.app with Clerk env vars configured</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify end-to-end auth and dashboard</name>
  <what-built>
Complete app shell: Next.js + Clerk auth + Neon connection + dashboard UI deployed to Vercel.

Before this checkpoint, Claude will have:
1. Linked to Vercel project and set all env vars
2. Deployed to production with `npx vercel --prod`
  </what-built>
  <how-to-verify>
    1. Open bea-os.vercel.app in browser
    2. Verify you see Clerk's sign-in page (not a blank screen or error)
    3. Log in with Bea's credentials — should see dashboard
    4. Verify nav shows "Bea's OS" and current week label (e.g., "Week of Apr 7-11, 2026")
    5. Verify Clerk UserButton avatar appears in top-right of nav
    6. Verify tab bar shows: Dashboard, Log Task, Jeff Tracker, Archive
    7. Verify dashboard shows 4 stat cards (all showing 0)
    8. Click each tab — verify placeholder pages load
    9. Click UserButton -> Sign out — verify redirect to sign-in page
    10. Log in with Sharie's credentials — same experience
    11. Test on mobile viewport (375px) — verify 2-column stat grid and no layout breakage
  </how-to-verify>
  <resume-signal>Type "approved" if everything works, or describe issues to fix</resume-signal>
  <done>App deployed to bea-os.vercel.app, both users can log in via Clerk, dashboard shell visible with all UI elements, sign-out works</done>
</task>

</tasks>

<verification>
- bea-os.vercel.app is live and accessible
- Unauthenticated visit redirects to Clerk sign-in
- Both Bea and Sharie can log in
- Dashboard shows nav + tabs + stat cards
- Sign-out via UserButton works
</verification>

<success_criteria>
Phase 1 is complete: live deployed app at bea-os.vercel.app with Clerk auth and dashboard shell. Both users can sign in, see stat cards (all zeros), navigate tabs, and sign out.
</success_criteria>

<output>
After completion, create `.planning/phases/01-shell-and-auth/01-03-SUMMARY.md`
</output>
